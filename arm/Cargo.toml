[package]
name = "arm"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# If you want to try (experimental) std support, add `features = [ "std" ]` to risc0-zkvm
risc0-zkvm = { version = "2.1.0", features = ["prove", "std", "unstable"] }
serde = { version = "1.0.197", default-features = false }
arm_core = { path = "../arm_core" }
k256 = { version = "=0.13.3", features = ["arithmetic", "serde", "expose-field", "std", "ecdsa", "hash2curve"], default-features = false }
rand = "0.8"
bincode = "1.3"
rustler = { version = "0.36.2", optional = true }

[dev-dependencies]

[features]
default = ["succinct_prover"]
nif = ["dep:rustler"]
fast_prover = []
composite_prover = []
succinct_prover = []
groth16_prover = []
