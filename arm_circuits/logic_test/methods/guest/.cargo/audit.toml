[advisories]
ignore = [
  # rsa 0.9.8 - Marvin Attack timing sidechannel. Transitive dep via
  # rzup -> risc0-build/risc0-groth16/risc0-zkvm. No fixed version exists upstream.
  "RUSTSEC-2023-0071",
  # tracing-subscriber 0.2.25 - ANSI escape log poisoning. Pinned to `^0.2` by
  # ark-relations 0.5.1 (via risc0-groth16 -> risc0-zkvm). Cannot bump without
  # an upstream risc0-zkvm release.
  "RUSTSEC-2025-0055",

  # --- unmaintained crates, all deep transitive deps via risc0-zkvm/ark, no fix available ---
  # atomic-polyfill 1.0.3, via heapless 0.7 -> postcard -> risc0-binfmt -> risc0-zkvm.
  "RUSTSEC-2023-0089",
  # derivative 2.2.0, via ark-crypto-primitives -> ark-groth16 -> risc0-groth16 -> risc0-zkvm.
  "RUSTSEC-2024-0388",
  # number_prefix 0.4.0, via indicatif -> circom-witnesscalc -> risc0-groth16 -> risc0-zkvm.
  "RUSTSEC-2025-0119",
  # paste 1.0.15, via alloy-sol-types -> arm_gadgets.
  "RUSTSEC-2024-0436",
  # proc-macro-error2 2.0.1, via alloy-sol-macro -> alloy-sol-types -> arm_gadgets.
  "RUSTSEC-2026-0173",

  # --- unmaintained direct dependency, migration deferred ---
  # bincode 1.3.3, used directly in arm/arm_gadgets to (de)serialize proofs and
  # resources (pulled into this workspace via path deps on those crates).
  # Migrating to bincode 2.x requires reworking ~15 call sites and verifying
  # wire-format compatibility with existing serialized data via the `legacy`
  # config - deferred as a dedicated follow-up rather than bundled here.
  "RUSTSEC-2025-0141",
]
