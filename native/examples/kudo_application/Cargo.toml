[workspace]
resolver = "2"
members = ["kudo_logic", "denomination_logic", "receive_logic", "kudo_core"]

# Always optimize; building and running the guest takes much longer without optimization.
[profile.dev]
opt-level = 3

[profile.release]
debug = 1
lto = true
